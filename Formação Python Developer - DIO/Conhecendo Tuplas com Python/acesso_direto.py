frutas = ("maçã", "laranja", "uva", "pera",)

print(frutas[0])  # maçã
print(frutas[2])  # uva

Explicação da função em tuplas:
Claro! Vamos analisar o código passo a passo:
1. Primeiro, criamos uma **tupla** chamada `frutas` com quatro elementos: `"maçã"`, `"laranja"`, `"uva"` e `"pera"`.
2. `print(frutas[0])`: Isso imprime o primeiro elemento da tupla, que é `"maçã"`.
3. `print(frutas[2])`: Aqui, acessamos o terceiro elemento da tupla, que é `"uva"`.
Em resumo, o código exibe os elementos da tupla `frutas` com base nos índices fornecidos. Espero que isso tenha esclarecido! 😊
