linguagens = (
    "python",
    "js",
    "c",
    "java",
    "csharp",
)

print(len(linguagens))  # 5

Explicação da função em tuplas:
Claro! Vamos analisar o código passo a passo:
1. Primeiro, criamos uma **tupla** chamada `linguagens` com cinco elementos: `"python"`, `"js"`, `"c"`, `"java"` e `"csharp"`.
2. `print(len(linguagens))`: Isso calcula o **comprimento** da tupla, ou seja, o número de elementos que ela contém. Neste caso, a tupla `linguagens` tem **cinco elementos**, então o resultado impresso será `5`.
Em resumo, o código utiliza a função `len()` para determinar quantos elementos estão presentes na tupla `linguagens`. Espero que isso tenha esclarecido! 😊
