frutas = (
    "maçã",
    "laranja",
    "uva",
    "pera",
)

print(frutas[-1])  # pera
print(frutas[-3])  # laranja

Explicação da função em tuplas:
Claro! Vamos analisar o código passo a passo:

1. Primeiro, criamos uma **tupla** chamada `frutas` com quatro elementos: `"maçã"`, `"laranja"`, `"uva"` e `"pera"`.
2. `print(frutas[-1])`: Isso imprime o último elemento da tupla, que é `"pera"`.
3. `print(frutas[-3])`: Aqui, acessamos o terceiro elemento a partir do final da tupla, que é `"laranja"`.
Em resumo, o código exibe os elementos da tupla `frutas` com base nos índices negativos, onde `-1` representa o último elemento, `-2` o penúltimo e assim por diante. Espero que isso tenha esclarecido! 😊
