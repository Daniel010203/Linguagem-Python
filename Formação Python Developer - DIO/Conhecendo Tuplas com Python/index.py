linguagens = ("python", "js", "c", "java", "csharp",)

print(linguagens.index("java"))  # 3
print(linguagens.index("python"))  # 0

Explicação da função em tuplas:
Claro! Vamos analisar o código passo a passo:

1. Primeiro, criamos uma **tupla** chamada `linguagens` com cinco elementos: `"python"`, `"js"`, `"c"`, `"java"` e `"csharp"`.
2. `print(linguagens.index("java"))`: Isso retorna o **índice** do elemento `"java"` na tupla. Como `"java"` está no terceiro lugar (índice 3), o resultado é `3`.
3. `print(linguagens.index("python"))`: Aqui, obtemos o índice do elemento `"python"`. Como `"python"` é o primeiro elemento (índice 0), o resultado é `0`.
Em resumo, o código utiliza o método `index()` para encontrar o índice de elementos específicos na tupla `linguagens`. Espero que isso tenha esclarecido! 😊
