cores = (
    "vermelho",
    "azul",
    "verde",
    "azul",
)

print(cores.count("vermelho"))  # 1
print(cores.count("azul"))  # 2
print(cores.count("verde"))  # 1

Explicação da função em tuplas:
Claro! Vamos analisar o código passo a passo:

1. Primeiro, criamos uma **tupla** chamada `cores` com quatro elementos: `"vermelho"`, `"azul"`, `"verde"` e `"azul"`.
2. `print(cores.count("vermelho"))`: Isso conta quantas vezes o elemento `"vermelho"` aparece na tupla. Neste caso, ele aparece apenas uma vez, então o resultado é `1`.
3. `print(cores.count("azul"))`: Aqui, contamos quantas vezes o elemento `"azul"` aparece na tupla. Ele aparece duas vezes, então o resultado é `2`.
4. `print(cores.count("verde"))`: Isso conta quantas vezes o elemento `"verde"` aparece na tupla. Novamente, ele aparece apenas uma vez, então o resultado é `1`.
Em resumo, o código utiliza o método `count()` para determinar a frequência de ocorrência de elementos específicos na tupla `cores`. Espero que isso tenha esclarecido! 😊
