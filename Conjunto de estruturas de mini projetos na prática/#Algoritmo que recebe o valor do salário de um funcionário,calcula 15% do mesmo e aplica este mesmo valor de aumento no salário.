#Algoritmo que recebe o valor do salário de um funcionário,calcula 15% do mesmo e aplica este mesmo valor de aumento no salário.
salario = float(input("Qual o valor do salário: "))
novoValorSalario = salario + (salario * 15/100)
print(" O salário reajustado em 15% passará a ser de R${:.2f}.".format(novoValorSalario))

#Código de reajuste salarial.
salario = float(input("Digite o salário do funcionário:R$  "))
novo_sala = salario + (salario * 15 / 100)
print("Um funcionário que ganhava R${:.2f}, com 15% de aumento,vai passar a receber R${:.2f}".format(salario,novo_sala))

